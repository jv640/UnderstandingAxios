{
	"name": "example",
	"version": "1.0.0",
	"main": "build/index.js",
	"scripts": {
		"build": "rimraf ./build && tsc",
		"dev": "nodemon",
		"start": "npm run build && node build/try.js"
	},
	"dependencies": {
		"axios": "^1.1.2",
		"milsymbol": "^2.0.0",
		"rimraf": "^3.0.2",
		"tsc": "^2.0.4",
		"typescript": "^4.8.4",
		"winston": "^3.8.2"
	},
	"devDependencies": {
		"@types/node": "^18.8.3"
	}
}
